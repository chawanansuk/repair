# Roadmap: ระบบซ่อมบำรุงหอพัก — ภาพรวมและแผนต่อยอด

เอกสารนี้ร้อยทุกชิ้นใน repo เข้าด้วยกัน: มีอะไรแล้ว ควรติดตั้งลำดับไหน
และจะไปต่อทางไหนได้อีก

## ภาพรวมระบบ (ข้อมูลไหลทางเดียว ชีตต้นทางไม่ถูกเขียนโดยระบบรายงาน)

```
ลูกน้องแจ้งงาน ──▶ ฟอร์ม (ระดับ 2) หรือ AppSheet (ระดับ 3) ──▶ แท็บ "งาน" ชีตหอพัก
                                                                    │
                                              dashboard เดิม ◀──────┤ (อ่าน)
                                                                    │
                                        ไฟล์รายงาน (อ่านอย่างเดียว) ◀┘
                                        ├─ รายงานรายเดือน (A/B/C)
                                        ├─ สรุปทั้งปี
                                        ├─ ตรวจข้อมูล            ← ต่อยอด v2
                                        ├─ 📧 PDF รายเดือนอัตโนมัติ ← ต่อยอด v2
                                        └─ ⚠️ แจ้งเตือนงานค้าง     ← ต่อยอด v2
```

## เฟส 1: ใช้งานได้จริง (ทำก่อน)

| ลำดับ | งาน | เวลา | เอกสาร |
|---|---|---|---|
| 1.1 | ติดตั้งไฟล์รายงาน (Apps Script) + ทดสอบเกณฑ์ตรวจรับ 5 ข้อ | 15 นาที | [README.md](README.md) |
| 1.2 | จัดระเบียบชีตต้นทาง: dropdown ประเภท/สถานะ/ตึก + ล็อกคอลัมน์ระบบ | 30 นาที | คำแนะนำ "ระดับ 1" |
| 1.3 | เลือกช่องทางกรอกของลูกน้อง: ฟอร์ม (ฟรี, แจ้งงานอย่างเดียว) หรือ AppSheet (ครบวงจร, อาจมีค่า license) | 15–60 นาที | [FORM-SETUP.md](FORM-SETUP.md) / [APPSHEET-SETUP.md](APPSHEET-SETUP.md) |

> ข้อ 1.2 สำคัญกว่าที่คิด — dropdown คือด่านกันข้อมูลเพี้ยนที่ถูกที่สุด
> ทุกระบบปลายน้ำ (dashboard, รายงาน, แจ้งเตือน) สุขภาพดีขึ้นทันที

## เฟส 2: ระบบทำงานแทนเรา (ต่อยอด v2 — โค้ดอยู่ใน `src/Code.gs` แล้ว)

เปิดใช้โดยตั้งค่าใน `CONFIG` แล้วรัน `setup` ใหม่หนึ่งครั้ง (ต้องอนุญาตสิทธิ์เพิ่ม
เพราะมีส่งอีเมล/สร้าง PDF):

| ฟีเจอร์ | ค่าที่ต้องตั้ง | พฤติกรรม |
|---|---|---|
| 📄 **PDF รายเดือนอัตโนมัติ** | `REPORT_RECIPIENTS` = อีเมลผู้รับ | ทุกวันที่ 1 เวลา 6 โมงเช้า ส่ง PDF รายงาน "เดือนก่อนหน้า" (A4 พร้อมพิมพ์/ส่งต่อเจ้าของหอ) — สั่งส่งเองได้จากเมนู 📄 ด้วย |
| ⚠️ **แจ้งเตือนงานค้าง** | `AGING_ALERT_DAYS` (ค่าเริ่มต้น 3 วัน, 0 = ปิด) | ทุกเช้าหลังรีเฟรชตี 5: ถ้ามีงานยังไม่เสร็จค้างเกินกำหนด ส่งอีเมลสรุปเรียงจากค้างนานสุด (ไม่เกินวันละฉบับ) |
| 🩺 **แท็บ "ตรวจข้อมูล"** | ไม่ต้องตั้ง — มาพร้อมรีเฟรช | ชี้แถวในชีตต้นทางที่มีปัญหา: วันที่อ่านไม่ได้ / ช่องห้องว่าง / งานเสร็จแต่ค่าใช้จ่ายว่าง พร้อมเลขแถวให้ไปแก้ที่ต้นทาง (ระบบไม่แก้ให้ — อ่านอย่างเดียวเหมือนเดิม) |

หลักการที่คงเดิมทุกฟีเจอร์: ชีตต้นทางถูก**อ่าน**ที่ `readSourceRows_()` จุดเดียว
ไม่มี write ใดๆ — งานค้าง/ปัญหาข้อมูลถูกตรวจจากรอบอ่านเดียวกับรายงาน ไม่เพิ่มภาระ

## เฟส 3: แนวทางต่อยอดถัดไป (ยังไม่ทำ — เรียงตามความคุ้ม)

1. **เก็บ PDF ลง Google Drive อัตโนมัติ** (ง่าย) — โฟลเดอร์ "รายงานย้อนหลัง"
   เดือนละไฟล์ เป็น archive ถาวร (ต้องเพิ่มสิทธิ์เขียน Drive)
2. **แจ้งเตือนเข้า LINE กลุ่ม** (กลาง) — LINE Notify ปิดบริการไปแล้ว (มี.ค. 2025)
   ต้องใช้ LINE Messaging API: สร้าง channel, ดึงกลุ่ม id, push message จาก
   Apps Script — ได้ผลลัพธ์เด้งเข้ามือถือทีมเร็วกว่าอีเมล แลกกับการตั้งค่าที่
   ยุ่งกว่าและโควตาข้อความฟรีจำกัด
3. **กราฟแนวโน้มในแท็บสรุปทั้งปี** (ง่าย–กลาง) — คอลัมน์ชาร์ตจำนวนงาน +
   เส้นค่าใช้จ่ายรายเดือน สร้างด้วยสคริปต์ตอนรีเฟรช
4. **สรุปค่าใช้จ่ายรายตึก/รายห้องสะสม** (กลาง) — แท็บใหม่ตอบคำถาม
   "ห้องไหนกินค่าซ่อมสูงผิดปกติ" ใช้ข้อมูลจากแคชเดิมได้เลย
5. **เกณฑ์งบประมาณ + แจ้งเมื่อเกิน** (ง่าย) — ตั้งงบต่อเดือนใน CONFIG
   ถ้าค่าใช้จ่ายสะสมเดือนนั้นทะลุ ส่งอีเมลเตือนทันทีที่รีเฟรชเจอ
6. **รวมหลายหอ** (ใหญ่) — ถ้ามีหอเพิ่ม: เปลี่ยน `SOURCE_SPREADSHEET_ID`
   เป็น array แล้ววนอ่านรวมในแคชเดียว เพิ่มคอลัมน์ "หอ" ในรายงาน

อยากได้ข้อไหน หยิบไปสั่งต่อได้ทีละข้อ — โครงโค้ดปัจจุบันรองรับการเสียบเพิ่ม
โดยไม่ต้องรื้อ (ทุกฟีเจอร์กินข้อมูลจาก `readSourceRows_()` / แคชกลางตัวเดียวกัน)

## สิ่งที่ตั้งใจ "ไม่ทำ"

- **แก้ไขชีตต้นทางอัตโนมัติ** (เช่น ระบบแก้วันที่ พ.ศ. ให้เอง) — เสี่ยงชน
  dashboard เดิมเกินคุ้ม จึงทำได้แค่ "ชี้จุด" ในแท็บตรวจข้อมูล
- **ยุ่งกับ Apps Script เดิมของชีตหอพัก** — ทุกอย่างอยู่โปรเจกต์แยกเสมอ
- **Webhook/แจ้งเตือนแบบเรียลไทม์ทันทีที่มีงานใหม่** — ต้องผูก trigger กับ
  ชีตต้นทางซึ่งผิดข้อห้าม (ทางเลือกที่ถูกกติกา: แจ้งเตือนแบบ digest จากฝั่ง
  ไฟล์รายงาน หรือใช้การแจ้งเตือนในตัวของ AppSheet ถ้าเลือกระดับ 3)
