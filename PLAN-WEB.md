# แผนงาน: เว็บรายงานซ่อมบำรุงออนไลน์ (เปิดดูจากมือถือ ไม่ต้องเข้าชีต)

## เป้าหมายและผู้ใช้

- **เจ้าของหอ / ผู้บริหาร**: เปิดลิงก์จากมือถือ เห็นรายงานเดือนล่าสุดทันที
  ไม่ต้องมีสิทธิ์เข้าไฟล์ Google Sheet ไม่ต้องรู้ว่า dropdown อยู่ตรงไหน
- **คุณ (ผู้ดูแล)**: ส่งลิงก์เดียวจบ ไม่ต้องแคปหน้าจอ/ส่ง PDF เองอีก
- **สิ่งที่ไม่เปลี่ยน**: ชีตหอพักยังถูก "อ่านอย่างเดียว" และทุกอย่างอยู่ใน
  โปรเจกต์สคริปต์ของไฟล์รายงานเดิม — ไม่มี server ใหม่ ไม่มีค่าใช้จ่ายเพิ่ม

## เลือกเทคโนโลยี: เทียบ 3 ทาง

| | A) Apps Script Web App ⭐ | B) เว็บแยก (Vercel/Pages) + API | C) ฝังชีตใน Google Sites |
|---|---|---|---|
| โครงสร้างเพิ่ม | ศูนย์ — ใช้โปรเจกต์สคริปต์รายงานเดิม | โฮสต์เว็บ + endpoint ข้อมูล + token | หน้า Sites |
| ค่าใช้จ่าย | ฟรี | ฟรีถึงหลักร้อย/เดือน | ฟรี |
| ความเป็นส่วนตัวข้อมูล | ล็อกอิน Google + allowlist อีเมล | ต้องเปิดช่องข้อมูลออก public หรือทำ auth เอง — **เสี่ยงชื่อ/เบอร์ลูกค้ารั่ว** | ตามสิทธิ์ไฟล์ชีต (หยาบ) |
| หน้าตา/มือถือ | ควบคุมได้เต็มที่ (HTML เราเขียนเอง) | ควบคุมได้เต็มที่ | ตารางชีตดิบๆ ไม่เหมาะมือถือ |
| การดูแล | โค้ดชุดเดียวกับระบบรายงาน | สองระบบ สอง repo สองที่พัง | ต่ำ แต่ได้ของหยาบ |

**เลือกทาง A** — เหตุผลชี้ขาดคือ (1) ข้อมูลมีชื่อ+เบอร์ลูกค้า จึงห้ามมี endpoint
สาธารณะเด็ดขาด ระบบ login ของ Google ฟรีและแข็งแรงกว่าที่เราจะเขียนเอง
(2) ใช้แคช `_data` และตรรกะสรุปที่มีอยู่แล้ว โค้ดเพิ่มไม่มาก ไม่มีระบบที่สองให้ดูแล

ข้อเสียของทาง A ที่ยอมรับ: URL ยาวไม่สวย (`script.google.com/macros/...`)
— แก้ทางได้ด้วยการทำ QR code / ย่อลิงก์ / บุ๊กมาร์กหน้าจอมือถือ

## สถาปัตยกรรม

```
ชีตหอพัก (อ่านอย่างเดียว)
   └─ refreshReport (ตี 5 + เมนู)  ──▶  _data (แคชในไฟล์รายงาน)
                                          │
        Google Sheet: รายงานรายเดือน ◀────┤  ตรรกะสรุปชุดเดียวกัน (aggregate*_)
                                          │
        เว็บ: doGet() ─ HtmlService  ◀────┘
        │  ผู้ใช้ล็อกอิน Google → เช็ค allowlist → render หน้า
        └─ URL: ?m=7&y=2026&view=monthly|yearly
```

จุดออกแบบสำคัญ: **เว็บอ่านจากแคช ไม่อ่านชีตต้นทางตรง** — โหลดเร็ว
(ไม่มี openById ตอนเปิดหน้า), ชีตต้นทางไม่โดนกระแทกทุกครั้งที่มีคนเปิดเว็บ
และเลขบนเว็บตรงกับในชีตเสมอเพราะมาจากแหล่งเดียวกัน

## ความปลอดภัย / ความเป็นส่วนตัว (หัวใจของแผนนี้)

1. **ข้อมูลลูกค้าไม่ขึ้นเว็บโดยสถาปัตยกรรม** — แคช `_data` เก็บแค่
   วันที่/ประเภท/ตึก/ห้อง/หมายเหตุ/ค่าใช้จ่าย ไม่เคยเก็บชื่อกับเบอร์
   ต่อให้โค้ดเว็บพลาด ข้อมูลติดต่อก็ไม่มีให้หลุด
2. **Deploy แบบ**: Execute as = Me · Who has access = **Anyone with Google account**
   (ไม่ใช้ "Anyone" เด็ดขาด)
3. **Allowlist ใน CONFIG**: `WEB_ALLOWED_EMAILS: ['เจ้าของหอ@gmail.com', ...]`
   — `doGet` เช็ค `Session.getActiveUser().getEmail()` ไม่อยู่ในลิสต์ →
   หน้าปฏิเสธสุภาพ ไม่โชว์ข้อมูลใดๆ (ลิสต์ว่าง = เปิดได้เฉพาะเจ้าของสคริปต์)
4. ปุ่มรีเฟรชข้อมูลบนเว็บ จำกัดเฉพาะอีเมลผู้ดูแล (`WEB_ADMIN_EMAILS`)

## หน้าจอ (mobile-first — เจ้าของหอเปิดจากมือถือเป็นหลัก)

ดีไซน์ตามพรีวิวที่ทำไว้แล้ว (artifact) ซึ่งพิสูจน์เลย์เอาต์ไปแล้ว:

- **แถบบน**: ชื่อหอ · เดือน/ปี (dropdown) · เวลารีเฟรชล่าสุด
- **การ์ดสรุป (ส่วน A)**: จำนวนงานเสร็จ + ค่าใช้จ่ายรวม ตัวใหญ่อ่านเร็ว
  แล้วค่อยตารางแยกประเภท
- **ส่วน B รายห้อง / ส่วน C ส่วนกลาง**: ตารางเดียวกับชีต แตะหัวข้อพับ/กางได้
- **แท็บสรุปทั้งปี**: ตาราง 12 เดือน (สกรอลล์แนวนอนในกรอบตัวเอง)
- ปุ่ม **🖨 พิมพ์ / บันทึก PDF** (ใช้ print stylesheet เดียวกับหน้าจอ)
- เปลี่ยนเดือน = โหลดผ่าน URL param (`?m=6&y=2026`) — แชร์ลิงก์เจาะเดือนได้

## แผนลงมือ (เรียงเป็นเฟส)

| เฟส | งาน | ประมาณเวลา |
|---|---|---|
| W1 | Refactor: แยกตรรกะสรุปเดือน/ปีใน `Code.gs` เป็นฟังก์ชันกลาง (`aggregateMonth_`, `aggregateYear_`) ให้ทั้งชีตและเว็บเรียกใช้ร่วมกัน — กันเลขสองหน้าจอไม่ตรงกัน | โค้ด 1–2 ชม. |
| W2 | เพิ่ม `doGet` + เทมเพลต HTML (มือถือ + print) + allowlist | โค้ด 2–3 ชม. |
| W3 | Deploy web app ครั้งแรก + เขียนคู่มือ `WEB-SETUP.md` (รวม gotcha สำคัญ: แก้โค้ดแล้วต้องกด **New version** ใน Manage deployments ไม่งั้นเว็บไม่อัปเดต) | 30 นาที |
| W4 | ทดสอบตามเกณฑ์ตรวจรับด้านล่าง → ส่งลิงก์/QR ให้เจ้าของหอ | 30 นาที |

## เกณฑ์ตรวจรับ

1. เปิดลิงก์จากมือถือด้วยอีเมลใน allowlist → เห็นรายงานเดือนปัจจุบัน อ่านง่าย ไม่ต้องซูม
2. อีเมลนอก allowlist → เห็นหน้าปฏิเสธ ไม่มีข้อมูลใดๆ หลุด
3. เปลี่ยนเดือนบนเว็บ → ตัวเลขตรงกับแท็บ "รายงานรายเดือน" ในชีตเดือนเดียวกันทุกบาท
4. ทั้งหน้าเว็บไม่มีชื่อลูกค้า/เบอร์โทรปรากฏที่ใดเลย (ตรวจทั้ง view-source)
5. กดพิมพ์จากเว็บ → ได้ A4 สะอาดเทียบเท่า PDF ที่ระบบส่งอีเมล
6. เปิดชีตหอพัก → ไม่มีการแก้ไขใดๆ เช่นเดิม (เว็บอ่านจากแคชเท่านั้น)

## สิ่งที่ตั้งใจไม่ทำในเวอร์ชันแรก

- **กรอกงานผ่านเว็บ** — ช่องทางกรอกมีแล้ว (ฟอร์ม/AppSheet) อย่าให้เว็บรายงาน
  กลายเป็นระบบเขียนข้อมูล ขอบเขตจะบานและเสี่ยงชนกติกาอ่านอย่างเดียว
- **กราฟ/แดชบอร์ดวิเคราะห์บนเว็บ** — รอเว็บนิ่งก่อนค่อยเพิ่ม (อยู่เฟส 3 ของ ROADMAP)
- **Custom domain** — ทำได้แต่ต้องมีโดเมน+ตัวกลาง proxy ไม่คุ้มกับผู้ใช้หลักหน่วยคน

พร้อมลงมือเมื่อไหร่ สั่งได้เลย — เฟส W1–W3 จบใน commit เดียวได้
