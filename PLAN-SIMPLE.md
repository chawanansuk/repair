# แผนทางเลือกแบบง่าย: สูตรล้วน ไม่ใช้ Apps Script

ทางเลือกของ [PLAN.md](PLAN.md) — ใช้ `IMPORTRANGE` + สูตร แทนสคริปต์ทั้งหมด
เหมาะเมื่อยอมรับข้อแลก 3 ข้อท้ายเอกสารนี้ได้

## เทียบกับแบบ Apps Script

| ประเด็น | Apps Script (PLAN.md) | สูตรล้วน (ไฟล์นี้) |
|---|---|---|
| ติดตั้ง | วางโค้ด 2 ไฟล์ + รัน `setup` + อนุญาตสิทธิ์ | สร้าง 4 แท็บ + วางสูตร + กด Allow access ครั้งเดียว |
| การรีเฟรช | ปุ่มเมนู + trigger ตี 5 | อัตโนมัติตลอด (~ทุก 30 นาที) ไม่ต้องมีปุ่ม/trigger |
| อ่านต้นทางอย่างเดียว | ต้องเชื่อว่าโค้ดไม่มี write call | IMPORTRANGE เขียนกลับไม่ได้โดยธรรมชาติ |
| เลย์เอาต์ A4 (3 ส่วนต่อกัน) | ไดนามิก สวยตามสเปก | ต้องจองพื้นที่คงที่ต่อส่วน มีช่องว่างตอนพิมพ์ |
| เรียงห้อง | natural sort (9 < 101, A2 < A10) | เรียงแบบข้อความ |
| ดูแลระยะยาว | ต้องอ่าน JavaScript เป็น | แก้สูตรในชีตได้เลย |

## แท็บที่ต้องสร้าง (ในไฟล์ Google Sheet ใหม่)

### 1) `_data` — ดึงข้อมูลดิบ (ซ่อนได้)

เซลล์ A1:

```
=IMPORTRANGE("ใส่_ID_ชีตหอพัก", "งาน!A1:L")
```

ครั้งแรกจะขึ้นปุ่ม **Allow access** — กดครั้งเดียวจบ หลังจากนั้นข้อมูล
อัปเดตเองตลอด (Google รีเฟรชให้ราวทุก 30 นาที และทุกครั้งที่เปิดไฟล์)

### 2) `_calc` — parse ข้อมูล (ซ่อนได้)

แถว 1 พิมพ์หัวเอง: `วันที่ | เดือน | ปี | ประเภท | ตึก | ห้อง | หมายเหตุ | ค่าใช้จ่าย | เสร็จ`

ทุกสูตรหา column จาก **ชื่อหัวตาราง** ด้วย `MATCH(...,_data!1:1,0)` — กันคอลัมน์สลับ
(ปรับ `2000` เป็นจำนวนแถวเผื่อที่ต้องการ)

**A2 — วันที่** (รับทั้งเซลล์ Date และข้อความ dd/MM/yyyy, ปี > 2400 ลบ 543):

```
=BYROW(INDEX(_data!A2:L2000,0,MATCH("วันที่",_data!1:1,0)),
  LAMBDA(v, IF(v="",,
    IF(ISNUMBER(v),
       IF(YEAR(v)>2400, DATE(YEAR(v)-543, MONTH(v), DAY(v)), v),
       IFERROR(LET(p, SPLIT(TO_TEXT(v),"/"),
         y0, VALUE(INDEX(p,1,3)),
         y,  IF(y0>2400, y0-543, y0),
         DATE(y, VALUE(INDEX(p,1,2)), VALUE(INDEX(p,1,1)))))))))
```

**B2 — เดือน / C2 — ปี:**

```
=ARRAYFORMULA(IF(A2:A2000="",, MONTH(A2:A2000)))
=ARRAYFORMULA(IF(A2:A2000="",, YEAR(A2:A2000)))
```

**D2 ประเภท / E2 ตึก / F2 ห้อง / G2 หมายเหตุ** (เปลี่ยนแค่ชื่อหัวในสูตร):

```
=ARRAYFORMULA(TRIM(TO_TEXT(INDEX(_data!A2:L2000,0,MATCH("ประเภท",_data!1:1,0)))))
```

**H2 — ค่าใช้จ่าย** (ว่าง = 0, รองรับ `1,250.50` / `฿300` / `200 บาท`):

```
=BYROW(INDEX(_data!A2:L2000,0,MATCH("ค่าใช้จ่าย",_data!1:1,0)),
  LAMBDA(v, IF(ISNUMBER(v), v,
    IFERROR(VALUE(REGEXREPLACE(TO_TEXT(v),"[,฿ ]|บาท","")), 0))))
```

**I2 — เสร็จ** (เสร็จ = "เสร็จ", "done", "ปิดแล้ว" ไม่สนตัวพิมพ์/ช่องว่าง):

```
=ARRAYFORMULA(REGEXMATCH(LOWER(TRIM(TO_TEXT(
   INDEX(_data!A2:L2000,0,MATCH("สถานะ",_data!1:1,0))))),
   "^(เสร็จ|done|ปิดแล้ว)$"))
```

### 3) `รายงานรายเดือน`

ส่วนควบคุม:

- **B2**: dropdown เดือน (Data validation → รายการ: มกราคม…ธันวาคม)
- **D2**: dropdown ปี (พิมพ์รายการปีที่ใช้ เช่น 2025, 2026)
- **H1** (ซ่อนได้) — แปลงชื่อเดือนเป็นเลข:

```
=MATCH(B2, {"มกราคม";"กุมภาพันธ์";"มีนาคม";"เมษายน";"พฤษภาคม";"มิถุนายน";
"กรกฎาคม";"สิงหาคม";"กันยายน";"ตุลาคม";"พฤศจิกายน";"ธันวาคม"}, 0)
```

**ส่วน A — สรุปภาพรวม** (วางที่ A5):

```
=IFERROR(QUERY({_calc!A2:I2000},
 "select Col4, count(Col4), sum(Col8)
  where Col9=true and Col2="&H1&" and Col3="&D2&"
  group by Col4 order by count(Col4) desc
  label Col4 'ประเภทงาน', count(Col4) 'จำนวนงานเสร็จ', sum(Col8) 'ค่าใช้จ่าย (บาท)'"),
 "— ไม่มีงานในเดือนนี้ —")
```

แถวรวมทั้งเดือน (ใต้ตาราง):

```
=COUNTIFS(_calc!B:B,H1, _calc!C:C,D2, _calc!I:I,TRUE)
=SUMIFS(_calc!H:H, _calc!B:B,H1, _calc!C:C,D2, _calc!I:I,TRUE)
```

**ส่วน B — รายห้อง** (จองพื้นที่คงที่ เช่น A15 ลงไป 60 แถว):

```
=IFERROR(SORT(FILTER(
   {_calc!A2:A2000, _calc!E2:E2000, _calc!F2:F2000,
    _calc!D2:D2000, _calc!G2:G2000, _calc!H2:H2000},
   _calc!I2:I2000, _calc!B2:B2000=H1, _calc!C2:C2000=D2,
   _calc!F2:F2000<>"ส่วนกลาง"),
 2,TRUE, 3,TRUE, 1,TRUE), "— ไม่มีงานรายห้อง —")
```

รวมค่าใช้จ่ายส่วน B:

```
=SUMIFS(_calc!H:H, _calc!B:B,H1, _calc!C:C,D2, _calc!I:I,TRUE, _calc!F:F,"<>ส่วนกลาง")
```

**ส่วน C — พื้นที่ส่วนกลาง** (เช่น A80 ลงไป): สูตรเดียวกับส่วน B
เปลี่ยนเงื่อนไขเป็น `_calc!F2:F2000="ส่วนกลาง"` และ SUMIFS เป็น `"ส่วนกลาง"`

### 4) `สรุปทั้งปี`

- **B2**: ปีที่ต้องการดู
- ตาราง: คอลัมน์ A = ชื่อเดือน (12 แถว), คอลัมน์ B–F = ประเภทหลัก 5 หมวด,
  G = อื่นๆ, H = รวมงาน, I = ค่าใช้จ่ายรวม

ต่อเซลล์ประเภท (แถวเดือนที่ n, หัวคอลัมน์ = ชื่อประเภท เช่น C$4 = "ซ่อม"):

```
=COUNTIFS(_calc!B:B, ROW()-4, _calc!C:C, $B$2, _calc!D:D, C$4, _calc!I:I, TRUE)
```

รวมงานทั้งเดือน (H) และ อื่นๆ (G = รวม − 5 หมวดหลัก):

```
=COUNTIFS(_calc!B:B, ROW()-4, _calc!C:C, $B$2, _calc!I:I, TRUE)
=H5-SUM(C5:F5)-B5
```

ค่าใช้จ่ายรวมต่อเดือน (I):

```
=SUMIFS(_calc!H:H, _calc!B:B, ROW()-4, _calc!C:C, $B$2, _calc!I:I, TRUE)
```

(`ROW()-4` = เลขเดือน เมื่อแถวมกราคมอยู่แถว 5 — ปรับ offset ตามตำแหน่งจริง)

## ข้อแลกที่ต้องยอมรับก่อนเลือกทางนี้

1. **เลย์เอาต์พิมพ์** — ส่วน B ยาวไม่เท่ากันแต่ละเดือน สูตรเลื่อนหัวส่วน C ตามไม่ได้
   ต้องจองแถวคงที่ต่อส่วน → หน้าพิมพ์มีช่องว่าง (ซ่อนแถวเปล่าเองก่อนพิมพ์ได้)
2. **เรียงห้องแบบข้อความ** — `A10` มาก่อน `A2` (ถ้าเลขห้องหลักเท่ากันหมด ไม่กระทบ)
3. **ปริมาณข้อมูล** — สูตร BYROW ระดับพันแถวสบาย แต่หลักหมื่นแถวจะเริ่มหน่วง

ถ้ารับ 3 ข้อนี้ไม่ได้ → ใช้แบบ Apps Script ตาม [PLAN.md](PLAN.md) + [src/Code.gs](src/Code.gs)
