# แผนงาน: ระบบรายงานงานซ่อมบำรุงรายเดือน

## เป้าหมาย

สร้าง Google Sheet **ไฟล์ใหม่แยกต่างหาก** พร้อม Apps Script ของตัวเอง
ที่ดึงข้อมูลจากชีตหอพัก (แท็บ "งาน") แบบ **อ่านอย่างเดียว** มาสร้าง:

1. แท็บ "รายงานรายเดือน" — เลือกเดือน/ปีจาก dropdown แล้วเห็นเฉพาะงานสถานะ "เสร็จ"
   แบ่ง 3 ส่วน (สรุปภาพรวม / รายห้อง / พื้นที่ส่วนกลาง) พิมพ์ A4 ได้สวย
2. แท็บ "สรุปทั้งปี" — ตาราง 12 เดือน × ประเภทงาน (จำนวน + ค่าใช้จ่ายรวมต่อเดือน)

## หลักการความปลอดภัยของชีตต้นทาง (ข้อห้ามข้อ 1–3)

- โค้ดเรียกชีตต้นทางที่ **จุดเดียว** ในฟังก์ชัน `readSourceRows_()` และใช้เฉพาะ
  `openById(...).getSheetByName('งาน').getDataRange().getValues()` — ไม่มีการเรียก
  method เขียนใดๆ (`setValue`, `clear`, `insertSheet`, ฯลฯ) กับ object ของชีตต้นทางเลย
- สคริปต์ทั้งหมดผูกกับ **ไฟล์ใหม่เท่านั้น** (สร้างผ่านเมนู ส่วนขยาย → Apps Script
  ของไฟล์ใหม่) ไม่ยุ่งกับสคริปต์เดิมของชีตต้นทางแม้แต่บรรทัดเดียว
- Trigger ทุกตัวสร้างในโปรเจกต์สคริปต์ของไฟล์ใหม่ ไม่มีผลกับ deployment เดิม

## สถาปัตยกรรม: ทำไมต้องมีแท็บแคช `_data` (ซ่อนไว้)

ปัญหา: การสลับเดือนด้วย dropdown ต้อง rebuild รายงานผ่าน trigger `onEdit`
แต่ **simple trigger ของ Google ไม่มีสิทธิ์เปิดสเปรดชีตไฟล์อื่น** (`openById` ใช้ไม่ได้)

ทางแก้: แยกเป็น 2 ชั้น

- **ชั้นดึงข้อมูล (refresh)** — เมนู "🔄 รีเฟรชรายงาน" หรือ trigger รายวันตี 5
  อ่านชีตต้นทาง → normalize (parse วันที่/พ.ศ., สถานะ, ค่าใช้จ่าย) →
  เก็บเฉพาะงาน "เสร็จ" ลงแท็บซ่อน `_data` ในไฟล์ใหม่ → rebuild ทั้งสองรายงาน
  → ประทับเวลารีเฟรชล่าสุด
- **ชั้นแสดงผล (onEdit)** — เมื่อผู้ใช้เปลี่ยน dropdown เดือน/ปี สคริปต์อ่านจาก
  `_data` (ไฟล์ตัวเอง ไม่ต้องขอสิทธิ์เพิ่ม) แล้ว rebuild เฉพาะแท็บนั้น — เร็วทันใจ
  และไม่ไปกวนชีตต้นทางทุกครั้งที่สลับเดือน

ผลพลอยได้: ชีตต้นทางถูกอ่านแค่วันละ 1 ครั้ง + ตอนกดรีเฟรชเอง ลดภาระทั้งสองฝั่ง

## การรับมือกับดักข้อมูล

| กับดัก | วิธีจัดการ |
|---|---|
| ตำแหน่งคอลัมน์อาจสลับ | หา index จากชื่อหัวตารางแถวแรก (`buildHeaderMap_`) ถ้าหัวที่ต้องใช้หาย → error บอกชื่อคอลัมน์ที่ขาด |
| วันที่เป็นทั้ง Date จริงและข้อความ dd/MM/yyyy | `parseJobDate_` รับทั้ง `instanceof Date` และ regex `d/M/yyyy` พร้อมตรวจวันจริง (กัน 31/02 ไหลข้ามเดือน) |
| ปี พ.ศ. ปนมา (เช่น 2569) | ถ้าปี > 2400 ให้ลบ 543 — ใช้กับทั้งเซลล์ Date และข้อความ |
| สถานะหลายแบบ | เทียบแบบ trim + lowercase: เสร็จ = "เสร็จ", "done", "ปิดแล้ว" (ยกเลิก/สถานะอื่นไม่เข้ารายงานอยู่แล้ว เพราะกรองเอาเฉพาะเสร็จ) |
| ประเภทนอกเหนือ 6 หมวด | สรุป (ส่วน A และสรุปทั้งปี) รวมเข้าหมวด "อื่นๆ" ส่วนรายแถว (B/C) แสดงค่าจริงตามต้นทาง |
| ห้อง = "ส่วนกลาง" | แยกไปส่วน C ไม่ปนกับรายห้อง |
| ค่าใช้จ่ายว่าง / มี , หรือ ฿ | `parseCost_` ล้างอักขระแล้ว parse, ว่างหรือ parse ไม่ได้ = 0 |

## เลย์เอาต์แท็บ "รายงานรายเดือน" (6 คอลัมน์ A–F พอดีกระดาษ A4 แนวตั้ง)

```
แถว 1  [ชื่อรายงาน — merge A:F]
แถว 2  เดือน: [dropdown]   ปี: [dropdown]        รีเฟรชล่าสุด: 22/07/2026 05:00
แถว 4  ── ส่วน A: สรุปภาพรวม ──
       ประเภท | จำนวนงานเสร็จ | ค่าใช้จ่าย  (เฉพาะประเภทที่มีงาน + แถวรวม)
       ── ส่วน B: งานรายห้อง ── (เรียง ตึก → ห้อง → วันที่)
       วันที่ | ตึก | ห้อง | ประเภท | รายละเอียด (หมายเหตุ) | ค่าใช้จ่าย  + แถวรวม
       ── ส่วน C: งานพื้นที่ส่วนกลาง ── (รูปแบบเดียวกับ B, ห้อง = "ส่วนกลาง") + แถวรวม
```

จัดรูปแบบ: หัวส่วนพื้นเข้ม/ตัวหนา เส้นตารางทุกส่วน ตัวเลขรูปแบบ #,##0.00
ปิด gridline เพื่อให้งานพิมพ์สะอาด กำหนดความกว้างคอลัมน์รวม ~ กว้าง A4

## เลย์เอาต์แท็บ "สรุปทั้งปี"

```
แถว 2  ปี: [dropdown]
ตาราง: เดือน | ซ่อม | ทำสะอาด | ชมห้อง | ย้ายเข้า | ย้ายออก | อื่นๆ | รวมงาน | ค่าใช้จ่ายรวม
       (ม.ค.–ธ.ค. 12 แถว + แถวรวมทั้งปี)
```

## การอัปเดตข้อมูล

- **เมนู custom** "🔄 รีเฟรชรายงาน" สร้างใน `onOpen`
- **Trigger รายวัน** ตี 5 เวลาไทย: `timeZone: "Asia/Bangkok"` ใน `appsscript.json`
  + `ScriptApp.newTrigger('refreshReport').timeBased().everyDays(1).atHour(5)`
  สร้างแบบ idempotent ในฟังก์ชัน `setup()` (ลบ trigger เดิมของ handler เดียวกันก่อน)
- **ประทับเวลา** รีเฟรชล่าสุดที่มุมขวาบนของรายงาน (อัปเดตเฉพาะตอนดึงข้อมูลจริง
  ไม่อัปเดตตอนแค่สลับเดือน)

## ขั้นตอนติดตั้ง (สรุป — ละเอียดใน README.md)

1. สร้าง Google Sheet ไฟล์ใหม่
2. ส่วนขยาย → Apps Script → วางโค้ด `Code.gs` + `appsscript.json`
3. ใส่ ID ชีตต้นทางใน `CONFIG.SOURCE_SPREADSHEET_ID`
4. รันฟังก์ชัน `setup` หนึ่งครั้ง → อนุญาตสิทธิ์ → ระบบสร้างแท็บ, dropdown,
   trigger รายวัน และดึงข้อมูลรอบแรกให้อัตโนมัติ

## แผนตรวจรับ (map กับเกณฑ์)

1. เปลี่ยน dropdown เดือน → onEdit rebuild จาก `_data` → รายงานเปลี่ยนตาม
2. เตรียมแถวทดสอบ 2 แบบ (เซลล์ Date จริง / ข้อความ dd/MM/yyyy เดือนเดียวกัน)
   → ต้องขึ้นครบทั้งคู่ (parser รองรับทั้งสอง + กรณี พ.ศ.)
3. งานห้อง "ส่วนกลาง" ต้องอยู่ส่วน C เท่านั้น (กรองด้วย `room === 'ส่วนกลาง'`)
4. แถวรวมค่าใช้จ่ายส่วน A = B + C และตรงกับบวกมือ (ช่องว่างนับเป็น 0)
5. ชีตต้นทาง: ตรวจ Version history ต้องไม่มี revision ใหม่จากสคริปต์นี้
   (โค้ดไม่มี write call ต่อไฟล์ต้นทางแม้แต่จุดเดียว)
