# ฟอร์มแจ้งงานซ่อมบำรุง → แท็บ "งาน" (คู่มือติดตั้งระดับ 2)

ให้ลูกน้องแจ้งงานใหม่ผ่าน Google Form จากมือถือ โดยไม่ต้องเปิดชีตหอพักเลย
สคริปต์สะพาน ([`src/form-bridge/Code.gs`](src/form-bridge/Code.gs)) จะ**ต่อท้าย**
แถวใหม่ในแท็บ "งาน" ให้อัตโนมัติ — append อย่างเดียว ไม่แก้แถวเดิม
และ**ไม่ยุ่งกับ Apps Script เดิม**ของชีตหอพักที่ dashboard ใช้อยู่
(สคริปต์ผูกกับตัวฟอร์ม เป็นคนละโปรเจกต์กัน)

> การปิดงาน (เปลี่ยนสถานะเป็น "เสร็จ" + ใส่ค่าใช้จ่าย) ยังทำในชีตตามเดิม
> แนะนำทำ dropdown ตาม "ระดับ 1" ควบคู่กัน

## ขั้นที่ 1: สร้างฟอร์ม (~10 นาที)

ไปที่ [forms.google.com](https://forms.google.com) → สร้างฟอร์มใหม่
ชื่อ "แจ้งงานซ่อมบำรุง" แล้วเพิ่มคำถามตามนี้ —
**หัวข้อคำถามต้องพิมพ์ให้ตรงตามตารางเป๊ะ** (สคริปต์จับคู่จากชื่อคำถาม):

| หัวข้อคำถาม | ชนิดคำถาม | ตัวเลือก / หมายเหตุ | จำเป็น |
|---|---|---|---|
| `ประเภทงาน` | เลือกตอบข้อเดียว (dropdown) | ซ่อม / ทำสะอาด / ชมห้อง / ย้ายเข้า / ย้ายออก / อื่นๆ | ✅ |
| `พื้นที่` | เลือกตอบข้อเดียว | ห้องพัก / ส่วนกลาง | ✅ |
| `ตึก` | เลือกตอบข้อเดียว (dropdown) | ใส่รายชื่อตึกจริงของหอ | ✅ |
| `เลขห้อง (ถ้าเป็นห้องพัก)` | คำตอบสั้นๆ | เว้นได้ถ้าเลือกพื้นที่ "ส่วนกลาง" | – |
| `ชื่อลูกค้า/ผู้แจ้ง` | คำตอบสั้นๆ | | – |
| `เบอร์ติดต่อ` | คำตอบสั้นๆ | | – |
| `รายละเอียดงาน` | คำตอบแบบยาว | เช่น "ซ่อม ก๊อกอ่างล้างหน้า", "ล้างแอร์" | ✅ |

เคล็ดลับ:

- ถ้าเลือกพื้นที่ "ส่วนกลาง" สคริปต์จะใส่คำว่า **ส่วนกลาง** ลงช่องห้องให้เอง
  (สะกดถูกเป๊ะเสมอ รายงานส่วน C กับ dashboard จับได้ชัวร์)
- ตั้งค่าฟอร์ม → การตอบกลับ → เปิด **รวบรวมที่อยู่อีเมล** ถ้าอยากให้ช่อง
  "ผู้สร้าง" เป็นอีเมลลูกน้องที่แจ้ง (ไม่เปิดก็ได้ จะบันทึกเป็น "ฟอร์มแจ้งงาน")
- **ไม่ต้องกดลิงก์ฟอร์มกับสเปรดชีต** (ปุ่ม Link to Sheets) — สคริปต์ส่งข้อมูล
  เข้าแท็บ "งาน" ตรงๆ อยู่แล้ว การลิงก์จะไปสร้างแท็บส่วนเกินในไฟล์ชีตหอพักเปล่าๆ

## ขั้นที่ 2: ติดตั้งสคริปต์สะพาน (~5 นาที)

1. ในหน้าแก้ไขฟอร์ม กดเมนู ⋮ (มุมขวาบน) → **ตัวแก้ไขสคริปต์ (Script editor)**
2. วางเนื้อหา [`src/form-bridge/Code.gs`](src/form-bridge/Code.gs) ทับ `Code.gs` เดิม
3. (แนะนำ) Project Settings → ติ๊ก *Show "appsscript.json"* แล้ววาง
   [`src/form-bridge/appsscript.json`](src/form-bridge/appsscript.json) ทับ
4. แก้ค่าบนสุดของไฟล์:
   - `SOURCE_SPREADSHEET_ID` — ID ชีตหอพัก (จาก URL ระหว่าง `/d/` กับ `/edit`)
   - `NEW_JOB_STATUS` — สถานะงานใหม่ ให้ตรงกับคำที่ dashboard เดิมใช้
     (ค่าเริ่มต้น: `รอดำเนินการ`)
5. กดบันทึก → เลือกฟังก์ชัน **`setup`** → **Run** → อนุญาตสิทธิ์
   (`setup` จะเช็กให้ด้วยว่าเปิดชีตหอพักได้และหัวคอลัมน์ครบ 12 ช่อง —
   ถ้าไม่ครบจะ error บอกชื่อคอลัมน์ที่หาไม่เจอ)
6. แจกลิงก์ฟอร์ม (ปุ่ม "ส่ง" → ไอคอนลิงก์) ให้ลูกน้อง — เซฟเป็น shortcut
   หน้าจอมือถือได้

## ขั้นที่ 3: ทดสอบ (เกณฑ์ตรวจรับ)

1. ส่งฟอร์มทดสอบแบบ "ห้องพัก" 1 รายการ → เปิดแท็บ "งาน" ต้องมีแถวใหม่ต่อท้าย
   ครบ: วันที่ (เซลล์ Date), ประเภท, ตึก, ห้อง, สถานะ = สถานะงานใหม่,
   วันที่สร้าง, id ขึ้นต้น `F-`
2. ส่งแบบ "ส่วนกลาง" 1 รายการ → ช่องห้องต้องเป็นคำว่า `ส่วนกลาง`
3. เบอร์โทรขึ้นต้นด้วย 0 → ศูนย์ต้องไม่หาย
4. แถวเดิมทั้งหมดในแท็บ "งาน" ต้องไม่ถูกแก้แม้แต่เซลล์เดียว และ dashboard
   เดิมยังทำงานปกติ
5. ปิดงานทดสอบในชีต (สถานะ → "เสร็จ" + ใส่ค่าใช้จ่าย) → กดรีเฟรชรายงาน
   งานต้องขึ้นในรายงานรายเดือนถูกส่วน (B หรือ C)

## ข้อควรรู้

- **รูปแบบ id**: สคริปต์สร้าง id เช่น `F-20260722-081530-3f2a` — ถ้า dashboard
  เดิมคาดหวัง id เป็นเลขรันต่อเนื่อง ให้แก้ฟังก์ชัน `generateId_` ให้ตรงก่อนใช้จริง
- ฟอร์มนี้**เพิ่มงานอย่างเดียว** ไม่มีทางไปลบ/แก้ข้อมูลเดิม (โค้ดมีแค่ `appendRow`)
- ลบงานทดสอบออกจากแท็บ "งาน" ได้ตามปกติหลังทดสอบเสร็จ
